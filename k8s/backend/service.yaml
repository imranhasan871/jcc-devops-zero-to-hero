apiVersion: v1
kind: Service
metadata:
  name: jcc-backend
  namespace: jcc-production
  labels:
    app: jcc-backend
spec:
  type: ClusterIP
  selector:
    app: jcc-backend
  ports:
    - name: http
      protocol: TCP
      port: 3000
      targetPort: 3000
